package es.medac.skycollectorapp;

import java.util.List;

// Clase modelo para recibir datos de la API (simulada por ahora)
public class FlightResponse {
    // Aquí irían los campos que devuelve la API de FlightRadar
    // Lo dejamos vacío o con una lista genérica para que no dé error
    public List<es.medac.skycollectorapp.Avion> data;
}
