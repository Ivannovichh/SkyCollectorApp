<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Tema base de la aplicación, heredando de Material3 sin ActionBar -->
    <style name="Base.Theme.SkyCollectorApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Aquí se pueden personalizar los colores y estilos del tema claro -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <!-- Tema principal de la aplicación que utiliza el tema base definido arriba -->
    <style name="Theme.SkyCollectorApp" parent="Base.Theme.SkyCollectorApp" />

</resources>
