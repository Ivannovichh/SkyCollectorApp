<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Tema base de la aplicación, heredando de Material3 sin ActionBar -->
    <style name="Base.Theme.SkyCollector" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Color principal de la aplicación -->
        <item name="colorPrimary">#0EA5E9</item>
    </style>

    <!-- Tema principal que utiliza el tema base definido arriba -->
    <style name="Theme.SkyCollector" parent="Base.Theme.SkyCollector" />

</resources>
